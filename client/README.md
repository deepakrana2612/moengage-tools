# Email Template Builder (React)

Drop your existing React app contents here.

After copying, make sure `client/package.json` has:

```json
{
  "homepage": "."
}
```

This ensures asset paths work correctly when served from `/template-builder`.
